name: 🏋️ 필라테스 자동 예약 v3.1

on:
  schedule:
    # 한국시간 23:45 (UTC 14:45) - GitHub Actions 지연을 고려하여 15분 앞당김
    # 실제 예약 실행은 자정(00:00~00:05) 사이에 이루어짐
    - cron: '45 14 * * *'  # UTC 14:45 = KST 23:45
  
  workflow_dispatch:
    inputs:
      test_mode:
        description: '테스트 모드 (실제 예약하지 않음)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      force_weekend:
        description: '주말에도 강제 실행 (테스트용)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  check-weekday:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
      target_date: ${{ steps.check.outputs.target_date }}
      day_name: ${{ steps.check.outputs.day_name }}
    steps:
    - name: 📅 주말 체크 (KST 기준) - 수정됨
      id: check
      run: |
        # 현재 UTC 시간을 KST로 변환하여 7일 후 계산
        echo "현재 UTC: $(date -u)"
        
        # KST 기준 현재 시간 (UTC + 9시간)
        current_kst=$(TZ='Asia/Seoul' date)
        echo "현재 KST: $current_kst"
        
        # KST 기준 현재 요일 확인
        current_day=$(TZ='Asia/Seoul' date '+%u')
        current_day_name=$(TZ='Asia/Seoul' date '+%A')
        echo "현재 요일: $current_day_name (숫자: $current_day)"
        
        # KST 기준 7일 후 날짜 계산
        target_date=$(TZ='Asia/Seoul' date -d '+7 days' '+%Y-%m-%d')
        target_day=$(TZ='Asia/Seoul' date -d '+7 days' '+%u') # 1=월요일, 7=일요일
        day_name_en=$(TZ='Asia/Seoul' date -d '+7 days' '+%A')
        
        # 한국어 요일 변환
        case $day_name_en in
          "Monday") day_name="월요일" ;;
          "Tuesday") day_name="화요일" ;;
          "Wednesday") day_name="수요일" ;;
          "Thursday") day_name="목요일" ;;
          "Friday") day_name="금요일" ;;
          "Saturday") day_name="토요일" ;;
          "Sunday") day_name="일요일" ;;
          *) day_name=$day_name_en ;;
        esac
        
        echo "🎯 예약 대상 날짜: $target_date ($day_name)"
        echo "📊 예약 대상 요일 숫자: $target_day (1=월, 7=일)"
        
        # 주말 체크 (토요일=6, 일요일=7)
        force_weekend="${{ github.event.inputs.force_weekend }}"
        if [[ "$target_day" == "6" || "$target_day" == "7" ]]; then
          if [[ "$force_weekend" == "true" ]]; then
            echo "⚠️ 주말이지만 강제 실행 옵션 활성화"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "🚫 주말($day_name)이므로 예약하지 않습니다"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi
        else
          echo "✅ 평일($day_name) 확인 - 예약 진행"
          echo "should_run=true" >> $GITHUB_OUTPUT
        fi
        
        echo "target_date=$target_date" >> $GITHUB_OUTPUT
        echo "day_name=$day_name" >> $GITHUB_OUTPUT

  wait-for-midnight:
    needs: check-weekday
    runs-on: ubuntu-latest
    if: needs.check-weekday.outputs.should_run == 'true'
    outputs:
      ready_for_booking: ${{ steps.wait.outputs.ready }}
    
    steps:
    - name: ⏰ 자정까지 대기
      id: wait
      run: |
        echo "⏰ 자정 대기 로직 시작"
        
        # 현재 KST 시간 확인
        current_kst=$(TZ='Asia/Seoul' date '+%H:%M:%S')
        current_hour=$(TZ='Asia/Seoul' date '+%H')
        current_minute=$(TZ='Asia/Seoul' date '+%M')
        
        echo "현재 KST 시간: $current_kst"
        echo "현재 시간: ${current_hour}시 ${current_minute}분"
        
        # 23:45 ~ 00:05 사이에만 실행 허용
        if [[ "$current_hour" == "23" && "$current_minute" -ge "45" ]]; then
          # 23:45 이후라면 자정까지 대기
          echo "🕚 23:45 이후 - 자정까지 대기 중..."
          
          while true; do
            now_hour=$(TZ='Asia/Seoul' date '+%H')
            now_minute=$(TZ='Asia/Seoul' date '+%M')
            now_second=$(TZ='Asia/Seoul' date '+%S')
            
            if [[ "$now_hour" == "00" && "$now_minute" == "00" ]]; then
              echo "🎯 자정 00:00 도달! 예약 시작"
              break
            elif [[ "$now_hour" == "00" && "$now_minute" -lt "05" ]]; then
              echo "🎯 자정 ${now_minute}분 - 예약 시작 (5분 이내)"
              break
            fi
            
            # 30초마다 체크
            sleep 30
            echo "⏳ 대기 중... 현재: ${now_hour}:${now_minute}:${now_second}"
          done
          
        elif [[ "$current_hour" == "00" && "$current_minute" -lt "05" ]]; then
          # 이미 자정~00:05 사이라면 바로 실행
          echo "🎯 현재 자정~00:05 사이 - 바로 예약 진행"
        else
          # 예상 밖의 시간대
          echo "⚠️ 예상 밖의 실행 시간: ${current_hour}:${current_minute}"
          echo "정상 실행 시간: 23:45~00:05"
          echo "그래도 예약 진행..."
        fi
        
        echo "ready=true" >> $GITHUB_OUTPUT
        echo "✅ 자정 대기 완료 - 예약 준비됨"

  pilates-booking:
    needs: [check-weekday, wait-for-midnight]
    runs-on: ubuntu-latest
    if: needs.check-weekday.outputs.should_run == 'true' && needs.wait-for-midnight.outputs.ready_for_booking == 'true'
    
    steps:
    - name: 📁 체크아웃
      uses: actions/checkout@v4
    
    - name: 🔧 Node.js 설정
      uses: actions/setup-node@v4
      with:
        node-version: '20'
    
    - name: 📦 의존성 설치
      run: npm install
    
    - name: 🔍 환경 확인
      run: |
        echo "📊 실행 환경:"
        echo "- Node.js: $(node --version)"
        echo "- npm: $(npm --version)"
        echo "- 타임존: $(date)"
        echo "- KST: $(TZ='Asia/Seoul' date)"
        echo "- 예약 날짜: ${{ needs.check-weekday.outputs.target_date }}"
        echo "- 요일: ${{ needs.check-weekday.outputs.day_name }}"
        echo "- 테스트 모드: ${{ github.event.inputs.test_mode }}"
    
    - name: 🏋️ 필라테스 예약 실행
      env:
        PILATES_USERNAME: ${{ secrets.PILATES_USERNAME }}
        PILATES_PASSWORD: ${{ secrets.PILATES_PASSWORD }}
        TEST_MODE: ${{ github.event.inputs.test_mode }}
        GITHUB_ACTIONS: true
        CI: true
      run: |
        if [[ "${{ github.event.inputs.test_mode }}" == "true" ]]; then
          echo "⚠️ 테스트 모드로 실행 중..."
          npm run book:test
        else
          echo "🎯 실제 예약 모드로 실행 중..."
          npm run book
        fi
    
    - name: 📋 결과 확인
      if: always()
      run: |
        echo "📊 예약 결과:"
        if [[ "${{ github.event.inputs.test_mode }}" == "true" ]]; then
          if [ -f "test-result.json" ]; then
            echo "✅ 테스트 결과 파일 생성됨"
            cat test-result.json
          else
            echo "❌ 테스트 결과 파일 없음"
          fi
        else
          if [ -f "booking-result.json" ]; then
            echo "✅ 예약 결과 파일 생성됨"
            cat booking-result.json
          else
            echo "❌ 예약 결과 파일 없음"
          fi
        fi
        
        echo ""
        echo "📁 로그 파일:"
        if [ -d "logs" ]; then
          ls -la logs/
          if [[ "${{ github.event.inputs.test_mode }}" == "true" ]]; then
            if [ -f "logs/test.log" ]; then
              echo "=== 테스트 로그 ==="
              tail -20 logs/test.log
            fi
          else
            if [ -f "logs/booking.log" ]; then
              echo "=== 예약 로그 ==="
              tail -20 logs/booking.log
            fi
          fi
        fi
    
    - name: 📸 스크린샷 업로드
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: booking-screenshots-${{ github.run_number }}
        path: |
          screenshots/
          logs/
          booking-result.json
          test-result.json
        retention-days: 7
        if-no-files-found: ignore

  weekend-skip:
    needs: check-weekday
    runs-on: ubuntu-latest
    if: needs.check-weekday.outputs.should_run == 'false'
    
    steps:
    - name: 🚫 주말 스킵 알림
      run: |
        echo "📅 예약 대상 날짜: ${{ needs.check-weekday.outputs.target_date }}"
        echo "📆 요일: ${{ needs.check-weekday.outputs.day_name }}"
        echo "🚫 주말이므로 예약을 건너뜁니다."
        echo "💡 주말에도 실행하려면 'force_weekend' 옵션을 true로 설정하세요."
